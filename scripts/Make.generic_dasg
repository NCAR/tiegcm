#
# Included makefile for generic NCAR/DASG machines
# (probably non-MPI serial)
#
# Compilers and flags:
#
F90      = ifort
MPIF90   = mpif90
FFLAGS   = -r8 -fixed -diag-disable remark
DBGFLAGS = 
OPTIM    = -O3
LIBS     = -lhdf5_hl -lhdf5 -lsz -L/usr/lib64 -lz -lcurl
#
# Library and Include file paths:
#
LIB_NETCDF = /fs/local/lib
INC_NETCDF = /fs/local/include
